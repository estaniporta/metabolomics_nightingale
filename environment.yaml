name: metabolomics_nightingale
channels:
  - conda-forge
  - defaults
dependencies:
  - r-base=4.4
  - r-irkernel          # R kernel for Jupyter
  - r-tidyverse
  - jupyterlab
  - notebook
# Some packages are not on conda-forge and will be installed
# via install.packages() in the notebook:
#   - ggforestplot  (GitHub: nightingalehealth/ggforestplot)
#   - nhanesA       (CRAN, added later for NHANES section)